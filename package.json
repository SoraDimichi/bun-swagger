{
  "name": "bun-swagger",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "tsoa-spec": "tsoa spec",
    "tsoa-routes": "tsoa routes",
    "generate": "bun run tsoa-spec && bun run tsoa-routes",
    "start": "bun run src/server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/cors": "^2.8.17"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "tsoa": "^6.6.0",
    "cors": "^2.8.5"
  }
}
